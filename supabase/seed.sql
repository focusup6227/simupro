-- Placeholder: `supabase/config.toml` references this file for `supabase db reset`.
-- Do not put secrets here. Auth users for agents/dev machines are created with:
--   npm run seed:test-users
-- (requires SUPABASE_SERVICE_ROLE_KEY + NEXT_PUBLIC_SUPABASE_URL in `.env.local`).

SELECT 1;
