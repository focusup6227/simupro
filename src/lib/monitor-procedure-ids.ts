/**
 * Procedure `id` values used by the monitor Proc menu (`interventions-data`) and
 * life-support priming. Keep in sync with `seedInterventions` / protocol catalog.
 */
export const MONITOR_PROCEDURE_CARDIOVERSION = 'cardioversion' as const;
export const MONITOR_PROCEDURE_DEFIBRILLATION = 'defibrillation' as const;
export const MONITOR_PROCEDURE_TCP = 'tcp' as const;
